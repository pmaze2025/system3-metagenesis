# Boot Flow

1. Open a new GPT session.
2. Upload the files from `/boot/` and `/canon/` if needed.
3. Paste `boot/BOOT.txt` into the session.
4. Wait for the expected STATE MIRROR posture.
5. Proceed with declarations and bounded work.

